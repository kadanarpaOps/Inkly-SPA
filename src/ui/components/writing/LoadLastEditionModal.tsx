import { useAuth } from "../../hooks/useAuth";
import { useStories } from "../../hooks/useStories"

const LoadLastEditionModal = () => {

  //const { authUser } = useAuth();
  //const { loading, loadLastModifiedStoryForAuthUser } = useStories();
  //
  //const responseStory = await loadLastModifiedStoryForAuthUser();


  return (
    <div>LoadLastEditionModal</div>
  )
}

export default LoadLastEditionModal
